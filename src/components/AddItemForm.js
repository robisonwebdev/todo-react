import React from 'react';

const AddItemForm = () => {
    return (
        <form>
            <input />
            <button>Add</button>
            <button>Cancel</button>
        </form>
    );
}

export default AddItemForm;