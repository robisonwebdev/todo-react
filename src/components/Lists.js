import React from 'react';
import '../styles/Lists.css';

const Lists = ({ title }) => {
    return (
        <div id='Lists'>
            <h1>{title}</h1>
        </div>
    );
}

export default Lists;