import React from 'react';
import '../styles/Header.css';

const Header = () => {
    return (
        <div id='Header'>
            <i class="las la-check-circle"></i>
            <h1>Todo List</h1>
        </div>
    );
}

export default Header;